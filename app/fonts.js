import { Inter } from 'next/font/google';
import { Noto_Serif_Bengali } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const kalpurushFont = Noto_Serif_Bengali({
  subsets: ['latin'],
});
